# CI run — nightly, 2026-09-22

One failure out of 412 tests.

```
FAIL tests/checkout/order-total.spec.ts:4
  @checkout @order-total shows the order total on the review step
  Error: expect(locator).toHaveText expected "$108.25"
  Received: "" (element rendered empty)
  server responded 500 on GET /api/cart/quote
```

The same job passed on the four previous nights. Nobody has opened dev since the run.
No deploy went out in the window. The CI worker's logs are rotated and the 500's body was
not captured.

Nothing else has been attempted: no manual reproduction, no local run, no screenshot.
