# Environment note — 2026-09-24, 09:10

Dev is mid-deploy. `https://dev.shop.acme.test/healthz` returns 503 and the deploy log
shows a migration still running. The platform team says two hours.

Staging is up and carries yesterday's build, which already includes the SHOP-260 change.
