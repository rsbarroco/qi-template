# Manual run — SHOP-236, dev, 2026-09-21

Executed by: the QA engineer, browser Chrome 141 on macOS, dev environment.
Reproduced by hand three times, same result each time.

```
FAIL tests/checkout/tax-total.spec.ts:7
  @checkout @tax adds 8.25% sales tax to the order total
  Expected: "$8.25"
  Received: "$0.00"
```

## Reproduction

1. Log in as `qa+tax@acme.test` at `https://dev.shop.acme.test`
2. Add the Anvil to the cart until the subtotal reads `$100.00`
3. Go to `/checkout/review`

**Expected** (AC1 of SHOP-236, verbatim): the order total includes sales tax at 8.25 % of
the subtotal, rounded to the cent.

**Actual:** the tax line renders with the label "Tax" and the value `$0.00`. The order
total reads `$100.00`.

## Evidence captured

- Screenshot: `runs/evidence/2026-09-21-review-tax-zero.png`
- Network: `GET /api/cart/quote` returns 200 with `"tax_cents": 0`, `"subtotal_cents": 10000`
- Browser console: no errors
- DB: `select tax_rate from tax_zones where zone = 'default'` returns `0.0825`

The rate is configured. The quote endpoint is the one returning zero.
