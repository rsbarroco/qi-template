# Bug drafts

A bug report is drafted here first, as `bugs/<TICKET>-<slug>.md`, and reviewed by the QA
engineer before it is filed in the tracker. The agent never files directly: `report-bug`
is marked `disable-model-invocation`, so filing is a human action.
