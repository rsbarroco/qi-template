# CI run — nightly, 2026-09-20

One failure out of 412 tests. No manual reproduction was attempted.

```
FAIL tests/checkout/gift-message.spec.ts:24
  @checkout @gift-message shows the gift message on the confirmation page
  TimeoutError: locator.waitFor: Timeout 5000ms exceeded.
  waiting for getByTestId('gift-message-value') to be visible
```

The same job passed on the two previous nights. Nobody has opened the dev environment
since.
