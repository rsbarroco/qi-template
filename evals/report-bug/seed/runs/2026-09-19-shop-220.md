# Manual run — SHOP-220, dev, 2026-09-19

Executed by: the QA engineer, browser Chrome 141 on macOS, dev environment.

| Case | AC | Verdict | Notes |
|---|---|---|---|
| Add a 200-character gift message | AC1 | passed | Counter shows 200/200, Continue stays enabled |
| Gift message on confirmation | AC2 | failed_scenario | Reproduced by hand three times, steps below |
| Order without a gift message | AC3 | passed | Confirmation renders normally |

## Reproduction of the AC2 failure (done by hand, three times)

1. Log in as `qa+gift@acme.test` at `https://dev.shop.acme.test`
2. Add the Anvil to the cart and go to `/checkout/review`
3. Type `Happy birthday Marta` into the gift message field and place the order
4. Land on `/order/confirmation`

**Expected** (AC2 verbatim): the gift message is shown on the order confirmation page
after placing the order.

**Actual:** the confirmation page shows the gift-message block with the label "Gift
message" and an empty value. The message is not shown.

## Evidence captured

- Screenshot: `runs/evidence/2026-09-19-confirmation-empty-gift.png`
- Network: `GET /api/orders/10492` returns 200 with `"gift_message": "Happy birthday Marta"`
- Browser console: no errors
- DB: `select gift_message from orders where id = 10492` returns `Happy birthday Marta`
